apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: {{ template "todo.fullname" . }}-todo
  name: postgres-data-pvc
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
